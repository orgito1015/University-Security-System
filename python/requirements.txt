# Runtime dependencies
# tkinter is part of the Python standard library; no extra install needed.
# PyQt6 is listed as an optional alternative GUI backend.
PyQt6>=6.6.0

# Development / testing dependencies
pytest>=7.4
